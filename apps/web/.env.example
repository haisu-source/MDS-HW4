# ---- Supabase (browser-safe) ----
# These two keys are PUBLIC on purpose. Row Level Security on Supabase keeps
# them safe; do NOT put the service role key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
